<div class="col-12 col-md mt guidelines">
  <h1>Guidelines</h1>
  <ul class="new-guidelines">
    <li>Search for your argument before entering a new statement. There may already be a perfect answer!</li>
    <li>Keep your statement short and direct</li>
  </ul>
</div>
<div class="col-12 col-md- d-flex justify-content-center">
  <%= simple_form_for [@argument] do |f| %>
    <%= f.input :content %>
    <%= f.input :source %>
    <%= f.association :tags, collection: Tag.order('name ASC'), as: :check_boxes, include_hidden: false %>
    <%= f.input :hidden, label: "Hide your username from this argument?", input_html: { class: "toggle" } %>
    <%= f.button :submit, value: "Add Argument", class: "btn btn-primary btn-lg w-100" %>
  <% end %>
</div>