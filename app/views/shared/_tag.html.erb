<% tag_name.first(2).each do |tag| %>
<%= link_to root_path + "search?search=#{tag.name}&search_results%5Bresults_ids%5D=&search_results%5Btag%5D=" + tag.name, class: "tag-selector" do %>
  <%= image_tag "Price Tag.svg", {width: 12} %>
  <%= tag.name %>
  <% end %>
<% end %>
